import 'package:flutter/material.dart';

class AdminRequestsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Certificate Requests'),
      ),
      body: Center(
        child: Text('Handle Certificate Requests Here'),
      ),
    );
  }
}
